# Session Summary — Feb 18 2026
# What we did today and where things stand

## What We Accomplished Today

Started with a broken DDML setup. Ended with a fully working ML simulation
pipeline, clean project structure, and GitHub backup. Specifically:

1. Diagnosed and fixed the DDML plugin loading failure
   - Root cause: libonnxruntime.so.1 and libtorch_cpu.so were not on
     LD_LIBRARY_PATH even though they exist in CVMFS
   - Fix: added two explicit export lines to source.sh pointing to the
     torch/lib and py-onnxruntime/lib64 directories in CVMFS

2. Fixed the model file path in the steer file
   - Changed ml_file from "../models/CC3_SF_2A.pt" to "models/CC3_SF_2A.pt"
   - The steer file now lives in steering/ and models/ is a sibling directory

3. Successfully ran CaloClouds3 ML simulation
   - 10 photon events, 10 GeV, direction (1,0,0)
   - Output: results/photon_gun_SIM_caloclouds.edm4hep.root
   - Basic ECAL energy plots confirmed working

4. Built clean project structure at ~/thesis-ml-sim/
   steering/        steer files
   analysis/        python scripts
   results/         ROOT output files
   models/          symlinks to .pt model weights in DDML install
   plots/           saved figures
   cognitive-state/ these markdown files
   docs/            setup documentation

5. Created source.sh — the one-command session ritual
   Run: source ~/source.sh
   Does: Key4hep + DDML + torch/onnx paths + navigation aliases + cd to workdir

6. Set up GitHub backup
   Repo: https://github.com/ad2912/Masters-Thesis-ML-Sim
   All steer files, analysis scripts, cognitive state backed up
   ROOT files are gitignored (large, regeneratable)


## Current State of Files

Steer files:
  steering/gamma_ML-ddsim_steer.py   CaloClouds3 ML simulation (WORKING)
  steering/gamma-ddsim_steer.py      Geant4 baseline (not yet tested from new structure)

Results so far:
  results/photon_gun_SIM_caloclouds.edm4hep.root   ML sim, 10 events
  results/photon_gun_SIM.edm4hep.root              Geant4, 10 events (from old structure)

Analysis scripts:
  analysis/inspect-sim.py        event-by-event inspection, ECAL/HCAL energy
  analysis/ecal-energy.py        ECAL energy plots
  analysis/gamma-distribution.py photon distribution plots


## How to Start Next Session

Step 1 — SSH into NAF:
  ssh -X alimuham@naf-ilc.desy.de

Step 2 — One command to set everything up:
  source ~/source.sh

Step 3 — Run simulation:
  ddsim --compactFile $k4geo_DIR/ILD/compact/ILD_l5_v02/ILD_l5_v02.xml \
        --steeringFile steering/gamma_ML-ddsim_steer.py \
        --outputFile results/photon_gun_SIM_caloclouds.edm4hep.root

Step 4 — Analyse:
  python3 analysis/inspect-sim.py results/photon_gun_SIM_caloclouds.edm4hep.root

Step 5 — Save work:
  git add .
  git commit -m "what you did"
  git push


## What To Do Next (in order)

1. Run Geant4 baseline from new structure to confirm it also works:
     ddsim --compactFile $k4geo_DIR/ILD/compact/ILD_l5_v02/ILD_l5_v02.xml \
           --steeringFile steering/gamma-ddsim_steer.py \
           --outputFile results/photon_gun_SIM_geant4.edm4hep.root

2. Write a comparison analysis script that reads both ROOT files and
   plots ML vs Geant4 side by side:
   - ECAL total energy distribution
   - Hit multiplicity
   - Shower lateral profile (hit positions in x-y)
   - Shower depth (hit positions along beam axis)

3. Increase statistics — 10 events is too few for meaningful distributions.
   Change SIM.numberOfEvents in the steer file to 100 or 500.

4. Understand what CaloClouds3 actually does physically:
   - It only triggers for e+, e-, gamma above 9.5 GeV in ECAL regions
   - Below threshold, Geant4 takes over automatically
   - It generates point clouds (x,y,z,E hits) rather than tracking particles


## Open Questions

- What happens to the 10 GeV photon if it hits outside EcalBarrelRegion
  or EcalEndcapRegion? Does it fall through to Geant4?
- How does CaloClouds3 handle the angular dependence of shower shape?
  (the TwoAngle in the model name suggests it accounts for theta and phi)
- Will need to read the CaloClouds3 paper before interpreting comparison plots

